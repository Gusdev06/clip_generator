# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Supabase Configuration
# Get your Project URL and Anon Key from: Project Settings > API
SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=your_supabase_anon_key

# YouTube Downloader Configuration

# Uses yt-dlp by default with cookies for authentication
# YouTube Cookies (REQUIRED for servers to avoid bot detection)
#
# Option 1: Use cookies from your browser (recommended for Docker)
# Values: chrome, firefox, edge, safari, etc.
# YT_COOKIES_FROM_BROWSER=chrome
#
# Option 2: Use a cookies.txt file (recommended for servers without browser)
# 1. Install browser extension: "Get cookies.txt LOCALLY" or similar
# 2. Go to youtube.com, make sure you're logged in
# 3. Export cookies to a file (Netscape format)
# 4. Upload to your server and set the path here:
# YT_COOKIES_FILE=/app/youtube_cookies.txt
